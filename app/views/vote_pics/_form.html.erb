<%= form_for(@vote_pic) do |f| %>
  <% if @vote_pic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote_pic.errors.count, "error") %> prohibited this vote_pic from being saved:</h2>

      <ul>
      <% @vote_pic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :src %><br>
    <%= f.text_field :src %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
