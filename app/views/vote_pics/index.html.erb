<table>
  <thead>
    <tr>
      <th>SrcSrc</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vote_pics.each do |vote_pic| %>
      <td>
        <tr><%= image_tag(vote_pic.src,art:vote_pic.src) %></tr>
        <tr><%= pluralize(vote_pics.votes.count, "vote") %></tr>
    		<tr><!--%= button_to '+1', upvote_vote_pic_path(vote_pic), method: :post %--></tr> 
       </td>
    <% end %>
  </tbody>
</table>


